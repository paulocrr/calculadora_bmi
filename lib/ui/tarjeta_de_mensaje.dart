import 'package:flutter/material.dart';

class TarjetaDeMensaje extends StatelessWidget {
  const TarjetaDeMensaje({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}